{
  "browser":[
    {
      "family":"chrome",
      "regex":"(chromium|chrome)/([0-9]+)\\.([0-9]+)\\.([0-9]+)",
      "description":"chrome desc"
    },{
      "family":"firefox",
      "regex":"(firefox)/([0-9]+)\\.([0-9]+)\\.([0-9]+)"
    }
  ],
  "os":[],
  "device":[
    {
      "family":"phone",
      "regex":"\\((ip[honed|\\s\\w*]+);.+(apple)",
      "description":"ipod/iphone"
    }
  ]
}